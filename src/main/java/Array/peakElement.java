package Array;

public class peakElement {

    public static void main(String[] args) {
        int[] a = {10, 2, 30, 45, 67, 89, 12, 23};
        int n = a.length;
        int result = peakElementMethod(a, n, 0, n - 1);
        System.out.println(result);
    }

    private static int peakElementMethod(int[] a, int n, int low, int high) {
//        int mid = low + (high - low) / 2;
//        if ((mid == 0 || a[mid - 1] <= a[mid]) && ())

     return 0;
    }
}
